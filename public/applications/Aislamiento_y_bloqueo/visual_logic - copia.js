/* eslint-disable */

/**
 * Generated by Verge3D Puzzles v.4.1.1
 * Thu, 12 Oct 2023 00:58:22 GMT
 * Prefer not editing this file as your changes may get overridden once Puzzles are saved.
 * Check out https://www.soft8soft.com/docs/manual/en/introduction/Using-JavaScript.html
 * for the information on how to add your own JavaScript to Verge3D apps.
 */

'use strict';

(function() {

// global variables/constants used by puzzles' functions

var LIST_NONE = '<none>';

var _pGlob = {};

_pGlob.objCache = {};
_pGlob.fadeAnnotations = true;
_pGlob.pickedObject = '';
_pGlob.hoveredObject = '';
_pGlob.mediaElements = {};
_pGlob.loadedFile = '';
_pGlob.states = [];
_pGlob.percentage = 0;
_pGlob.openedFile = '';
_pGlob.openedFileMeta = {};
_pGlob.xrSessionAcquired = false;
_pGlob.xrSessionCallbacks = [];
_pGlob.screenCoords = new v3d.Vector2();
_pGlob.intervalTimers = {};
_pGlob.customEvents = new v3d.EventDispatcher();
_pGlob.eventListeners = [];

_pGlob.AXIS_X = new v3d.Vector3(1, 0, 0);
_pGlob.AXIS_Y = new v3d.Vector3(0, 1, 0);
_pGlob.AXIS_Z = new v3d.Vector3(0, 0, 1);
_pGlob.MIN_DRAG_SCALE = 10e-4;
_pGlob.SET_OBJ_ROT_EPS = 1e-8;

_pGlob.vec2Tmp = new v3d.Vector2();
_pGlob.vec2Tmp2 = new v3d.Vector2();
_pGlob.vec3Tmp = new v3d.Vector3();
_pGlob.vec3Tmp2 = new v3d.Vector3();
_pGlob.vec3Tmp3 = new v3d.Vector3();
_pGlob.vec3Tmp4 = new v3d.Vector3();
_pGlob.eulerTmp = new v3d.Euler();
_pGlob.eulerTmp2 = new v3d.Euler();
_pGlob.quatTmp = new v3d.Quaternion();
_pGlob.quatTmp2 = new v3d.Quaternion();
_pGlob.colorTmp = new v3d.Color();
_pGlob.mat4Tmp = new v3d.Matrix4();
_pGlob.planeTmp = new v3d.Plane();
_pGlob.raycasterTmp = new v3d.Raycaster(); // always check visibility

var PL = {};
// backward compatibility
if (v3d[Symbol.toStringTag] !== 'Module') {
    v3d.PL = v3d.puzzles = PL;
}

PL.procedures = PL.procedures || {};




PL.execInitPuzzles = function(options) {
    // always null, should not be available in "init" puzzles
    var appInstance = null;
    // app is more conventional than appInstance (used in exec script and app templates)
    var app = null;

    var _initGlob = {};
    _initGlob.percentage = 0;
    _initGlob.output = {
        initOptions: {
            fadeAnnotations: true,
            useBkgTransp: false,
            preserveDrawBuf: false,
            useCompAssets: false,
            useFullscreen: true,
            useCustomPreloader: false,
            preloaderStartCb: function() {},
            preloaderProgressCb: function() {},
            preloaderEndCb: function() {},
        }
    }

    // provide the container's id to puzzles that need access to the container
    _initGlob.container = options !== undefined && 'container' in options
            ? options.container : "";

    

    // utility functions envoked by the HTML puzzles
function getElements(ids, isParent) {
    var elems = [];
    if (Array.isArray(ids) && ids[0] != 'CONTAINER' && ids[0] != 'WINDOW' &&
        ids[0] != 'DOCUMENT' && ids[0] != 'BODY' && ids[0] != 'QUERYSELECTOR') {
        for (var i = 0; i < ids.length; i++)
            elems.push(getElement(ids[i], isParent));
    } else {
        elems.push(getElement(ids, isParent));
    }
    return elems;
}

function getElement(id, isParent) {
    var elem;
    if (Array.isArray(id) && id[0] == 'CONTAINER') {
        if (appInstance !== null) {
            elem = appInstance.container;
        } else if (typeof _initGlob !== 'undefined') {
            // if we are on the initialization stage, we still can have access
            // to the container element
            var id = _initGlob.container;
            if (isParent) {
                elem = parent.document.getElementById(id);
            } else {
                elem = document.getElementById(id);
            }
        }
    } else if (Array.isArray(id) && id[0] == 'WINDOW') {
        if (isParent)
            elem = parent;
        else
            elem = window;
    } else if (Array.isArray(id) && id[0] == 'DOCUMENT') {
        if (isParent)
            elem = parent.document;
        else
            elem = document;
    } else if (Array.isArray(id) && id[0] == 'BODY') {
        if (isParent)
            elem = parent.document.body;
        else
            elem = document.body;
    } else if (Array.isArray(id) && id[0] == 'QUERYSELECTOR') {
        if (isParent)
            elem = parent.document.querySelector(id);
        else
            elem = document.querySelector(id);
    } else {
        if (isParent)
            elem = parent.document.getElementById(id);
        else
            elem = document.getElementById(id);
    }
    return elem;
}

// setHTMLElemAttribute puzzle
function setHTMLElemAttribute(attr, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem) continue;

        if ((attr == 'href' || attr == 'src') && value instanceof Promise) {
            // resolve promise value for url-based attributes
            value.then(function(response) {
                elem[attr] = response;
            });
        } else {
            elem[attr] = value;
        }
    }
}

// setHTMLElemStyle puzzle
function setHTMLElemStyle(prop, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem || !elem.style)
            continue;
        elem.style[prop] = value;
    }
}



// initPreloader puzzle
_initGlob.output.initOptions.useCustomPreloader = true;
_initGlob.output.initOptions.preloaderStartCb = function() {
    _initGlob.percentage = 0;
    (function() {})();
};
_initGlob.output.initOptions.preloaderProgressCb = function(percentage) {
    _initGlob.percentage = percentage;
    (function() {
  setHTMLElemAttribute('innerHTML', String(Math.round(_initGlob.percentage)) + '%', 'percentage', false);
})();
};
_initGlob.output.initOptions.preloaderEndCb = function() {
    _initGlob.percentage = 100;
    (function() {
  setHTMLElemStyle('display', 'none', 'preloader_screen', false);
  setHTMLElemStyle('display', 'flex', 'info_screen', false);
  setHTMLElemStyle('display', 'flex', 'titulo_container', false);
  setHTMLElemStyle('display', 'flex', 'powered_by_eduverso_img', false);
  setHTMLElemStyle('display', 'none', 'btn_next_1', false);
})();
};

    return _initGlob.output;
}

PL.init = function(appInstance, initOptions) {

// app is more conventional than appInstance (used in exec script and app templates)
var app = appInstance;

initOptions = initOptions || {};

if ('fadeAnnotations' in initOptions) {
    _pGlob.fadeAnnotations = initOptions.fadeAnnotations;
}



var PROC = {
    
};

var VARS = Object.defineProperties({}, {
    'is_ft_loaded': { get: function() { return is_ft_loaded; }, set: function(val) { is_ft_loaded = val; } },
    'is_press_red_button_apronfeeder': { get: function() { return is_press_red_button_apronfeeder; }, set: function(val) { is_press_red_button_apronfeeder = val; } },
    'is_bajar_palanca_apronfeeder': { get: function() { return is_bajar_palanca_apronfeeder; }, set: function(val) { is_bajar_palanca_apronfeeder = val; } },
    'is_press_tapa_lonchera': { get: function() { return is_press_tapa_lonchera; }, set: function(val) { is_press_tapa_lonchera = val; } },
    'is_bajar_palanca_electroiman': { get: function() { return is_bajar_palanca_electroiman; }, set: function(val) { is_bajar_palanca_electroiman = val; } },
});

var is_ft_loaded, is_press_red_button_apronfeeder, is_bajar_palanca_apronfeeder, is_press_tapa_lonchera, is_bajar_palanca_electroiman;

// utility functions envoked by the HTML puzzles
function getElements(ids, isParent) {
    var elems = [];
    if (Array.isArray(ids) && ids[0] != 'CONTAINER' && ids[0] != 'WINDOW' &&
        ids[0] != 'DOCUMENT' && ids[0] != 'BODY' && ids[0] != 'QUERYSELECTOR') {
        for (var i = 0; i < ids.length; i++)
            elems.push(getElement(ids[i], isParent));
    } else {
        elems.push(getElement(ids, isParent));
    }
    return elems;
}

function getElement(id, isParent) {
    var elem;
    if (Array.isArray(id) && id[0] == 'CONTAINER') {
        if (appInstance !== null) {
            elem = appInstance.container;
        } else if (typeof _initGlob !== 'undefined') {
            // if we are on the initialization stage, we still can have access
            // to the container element
            var id = _initGlob.container;
            if (isParent) {
                elem = parent.document.getElementById(id);
            } else {
                elem = document.getElementById(id);
            }
        }
    } else if (Array.isArray(id) && id[0] == 'WINDOW') {
        if (isParent)
            elem = parent;
        else
            elem = window;
    } else if (Array.isArray(id) && id[0] == 'DOCUMENT') {
        if (isParent)
            elem = parent.document;
        else
            elem = document;
    } else if (Array.isArray(id) && id[0] == 'BODY') {
        if (isParent)
            elem = parent.document.body;
        else
            elem = document.body;
    } else if (Array.isArray(id) && id[0] == 'QUERYSELECTOR') {
        if (isParent)
            elem = parent.document.querySelector(id);
        else
            elem = document.querySelector(id);
    } else {
        if (isParent)
            elem = parent.document.getElementById(id);
        else
            elem = document.getElementById(id);
    }
    return elem;
}

// setHTMLElemStyle puzzle
function setHTMLElemStyle(prop, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem || !elem.style)
            continue;
        elem.style[prop] = value;
    }
}

function _checkListenersSame(target0, type0, listener0, optionsOrUseCapture0,
        target1, type1, listener1, optionsOrUseCapture1) {
    const capture0 = Boolean(optionsOrUseCapture0 instanceof Object
            ? optionsOrUseCapture0.capture : optionsOrUseCapture0);
    const capture1 = Boolean(optionsOrUseCapture1 instanceof Object
            ? optionsOrUseCapture1.capture : optionsOrUseCapture1);
    return target0 === target1 && type0 === type1 && listener0 === listener1
            && capture0 === capture1;
}

/**
 * Add the specified event listener to the specified target. This function also
 * stores listener data for easier disposing.
 */
function bindListener(target, type, listener, optionsOrUseCapture) {
    const alreadyExists = _pGlob.eventListeners.some(elem => {
        return _checkListenersSame(elem.target, elem.type, elem.listener,
                elem.optionsOrUseCapture, target, type, listener,
                optionsOrUseCapture);
    });

    if (!alreadyExists) {
        target.addEventListener(type, listener, optionsOrUseCapture);
        _pGlob.eventListeners.push({ target, type, listener, optionsOrUseCapture });
    }
}

// eventHTMLElem puzzle
function eventHTMLElem(eventType, ids, isParent, callback) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem)
            continue;

        bindListener(elem, eventType, callback);
    }
}

function MediaHTML5(isVideo) {
    this.source = null;
}

Object.assign(MediaHTML5.prototype, {

    load: function(url, isVideo) {
        if (isVideo) {
            this.source = document.createElement('video');
            this.source.playsInline = true;
            this.source.preload = 'auto';
            this.source.autoload = true;
            this.source.crossOrigin = 'anonymous';
        } else {
            this.source = document.createElement('audio');
        }

        this.source.src = url;
        return this;
    },

    play: function() {
        this.source.play();
    },

    pause: function() {
        this.source.pause();
    },

    stop: function() {
        this.source.pause();
        this.source.currentTime = 0;
    },

    rewind: function() {
        this.source.currentTime = 0;
    },

    setPlaybackTime: function(time) {
        this.source.currentTime = time
    },

    getPlaybackTime: function() {
        return this.source.currentTime;
    },

    getDuration: function() {
        return this.source.duration;
    },

    setPlaybackRate: function(rate) {
        this.source.playbackRate = rate;
    },

    isPlaying: function() {
        return this.source.duration > 0 && !this.source.paused;
    },

    setLoop: function(looped) {
        this.source.loop = looped;
    },

    setVolume: function(volume) {
        this.source.volume = volume;
    },

    setMuted: function(muted) {
        this.source.muted = muted;
    },

});

// loadMedia puzzle
function loadMedia_HTML5(url) {

    var elems = _pGlob.mediaElements;
    if (!(url in elems)) {
        elems[url] = new MediaHTML5().load(url);
    }
    return elems[url];
}

// playSound puzzle
function playSound(mediaElem, loop) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);
    mediaElems.forEach(mediaElem => {
        mediaElem.setLoop(loop);
        mediaElem.play();
    });
}

// volume puzzle
function volume(mediaElem, volume) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);

    volume = Number(volume);
    if (Number.isNaN(volume)) {
        return;
    }

    mediaElems.forEach(mediaElem => {
        mediaElem.setVolume(v3d.MathUtils.clamp(volume, 0.0, 1.0));
    });
}

// setTimeout puzzle
function registerSetTimeout(timeout, callback) {
    window.setTimeout(callback, 1000 * timeout);
}

// utility function envoked by almost all V3D-specific puzzles
// filter off some non-mesh types
function notIgnoredObj(obj) {
    return obj.type !== 'AmbientLight' &&
           obj.name !== '' &&
           !(obj.isMesh && obj.isMaterialGeneratedMesh) &&
           !obj.isAuxClippingMesh;
}


// utility function envoked by almost all V3D-specific puzzles
// find first occurence of the object by its name
function getObjectByName(objName) {
    var objFound;
    var runTime = _pGlob !== undefined;
    objFound = runTime ? _pGlob.objCache[objName] : null;

    if (objFound && objFound.name === objName)
        return objFound;

    if (appInstance.scene) {
        appInstance.scene.traverse(function(obj) {
            if (!objFound && notIgnoredObj(obj) && (obj.name == objName)) {
                objFound = obj;
                if (runTime) {
                    _pGlob.objCache[objName] = objFound;
                }
            }
        });
    }
    return objFound;
}


// utility function envoked by almost all V3D-specific puzzles
// retrieve all objects on the scene
function getAllObjectNames() {
    var objNameList = [];
    appInstance.scene.traverse(function(obj) {
        if (notIgnoredObj(obj))
            objNameList.push(obj.name)
    });
    return objNameList;
}


// utility function envoked by almost all V3D-specific puzzles
// retrieve all objects which belong to the group
function getObjectNamesByGroupName(targetGroupName) {
    var objNameList = [];
    appInstance.scene.traverse(function(obj){
        if (notIgnoredObj(obj)) {
            var groupNames = obj.groupNames;
            if (!groupNames)
                return;
            for (var i = 0; i < groupNames.length; i++) {
                var groupName = groupNames[i];
                if (groupName == targetGroupName) {
                    objNameList.push(obj.name);
                }
            }
        }
    });
    return objNameList;
}


// utility function envoked by almost all V3D-specific puzzles
// process object input, which can be either single obj or array of objects, or a group
function retrieveObjectNames(objNames) {
    var acc = [];
    retrieveObjectNamesAcc(objNames, acc);
    return acc.filter(function(name) {
        return name;
    });
}

function retrieveObjectNamesAcc(currObjNames, acc) {
    if (typeof currObjNames == "string") {
        acc.push(currObjNames);
    } else if (Array.isArray(currObjNames) && currObjNames[0] == "GROUP") {
        var newObj = getObjectNamesByGroupName(currObjNames[1]);
        for (var i = 0; i < newObj.length; i++)
            acc.push(newObj[i]);
    } else if (Array.isArray(currObjNames) && currObjNames[0] == "ALL_OBJECTS") {
        var newObj = getAllObjectNames();
        for (var i = 0; i < newObj.length; i++)
            acc.push(newObj[i]);
    } else if (Array.isArray(currObjNames)) {
        for (var i = 0; i < currObjNames.length; i++)
            retrieveObjectNamesAcc(currObjNames[i], acc);
    }
}

// outline puzzle
function outline(objSelector, doWhat) {
    var objNames = retrieveObjectNames(objSelector);

    if (!appInstance.postprocessing || !appInstance.postprocessing.outlinePass)
        return;
    var outlineArray = appInstance.postprocessing.outlinePass.selectedObjects;
    for (var i = 0; i < objNames.length; i++) {
        var objName = objNames[i];
        var obj = getObjectByName(objName);
        if (!obj)
            continue;
        if (doWhat == "ENABLE") {
            if (outlineArray.indexOf(obj) == -1)
                outlineArray.push(obj);
        } else {
            var index = outlineArray.indexOf(obj);
            if (index > -1)
                outlineArray.splice(index, 1);
        }
    }
}

/**
 * Retrieve coordinate system from the loaded scene
 */
function getCoordSystem() {
    var scene = appInstance.scene;

    if (scene && 'coordSystem' in scene.userData) {
        return scene.userData.coordSystem;
    }

    return 'Y_UP_RIGHT';
}


/**
 * Transform coordinates from one space to another
 * Can be used with Vector3 or Euler.
 */
function coordsTransform(coords, from, to, noSignChange) {

    if (from == to)
        return coords;

    var y = coords.y, z = coords.z;

    if (from == 'Z_UP_RIGHT' && to == 'Y_UP_RIGHT') {
        coords.y = z;
        coords.z = noSignChange ? y : -y;
    } else if (from == 'Y_UP_RIGHT' && to == 'Z_UP_RIGHT') {
        coords.y = noSignChange ? z : -z;
        coords.z = y;
    } else {
        console.error('coordsTransform: Unsupported coordinate space');
    }

    return coords;
}


/**
 * Verge3D euler rotation to Blender/Max shortest.
 * 1) Convert from intrinsic rotation (v3d) to extrinsic XYZ (Blender/Max default
 *    order) via reversion: XYZ -> ZYX
 * 2) swizzle ZYX->YZX
 * 3) choose the shortest rotation to resemble Blender's behavior
 */
const eulerV3DToBlenderShortest = function() {

    const eulerTmp = new v3d.Euler();
    const eulerTmp2 = new v3d.Euler();
    const vec3Tmp = new v3d.Vector3();

    return function(euler, dest) {

        const eulerBlender = eulerTmp.copy(euler).reorder('YZX');
        const eulerBlenderAlt = eulerTmp2.copy(eulerBlender).makeAlternative();

        const len = vec3Tmp.setFromEuler(eulerBlender).lengthSq();
        const lenAlt = vec3Tmp.setFromEuler(eulerBlenderAlt).lengthSq();

        dest.copy(len < lenAlt ? eulerBlender : eulerBlenderAlt);
        return coordsTransform(dest, 'Y_UP_RIGHT', 'Z_UP_RIGHT');
    }

}();

// tweenCamera puzzle
function tweenCamera(posOrObj, targetOrObj, duration, doSlot, movementType) {
    var camera = appInstance.getCamera();

    if (Array.isArray(posOrObj)) {
        var worldPos = _pGlob.vec3Tmp.fromArray(posOrObj);
        worldPos = coordsTransform(worldPos, getCoordSystem(), 'Y_UP_RIGHT');
    } else if (posOrObj) {
        var posObj = getObjectByName(posOrObj);
        if (!posObj) return;
        var worldPos = posObj.getWorldPosition(_pGlob.vec3Tmp);
    } else {
        // empty input means: don't change the position
        var worldPos = camera.getWorldPosition(_pGlob.vec3Tmp);
    }

    if (Array.isArray(targetOrObj)) {
        var worldTarget = _pGlob.vec3Tmp2.fromArray(targetOrObj);
        worldTarget = coordsTransform(worldTarget, getCoordSystem(), 'Y_UP_RIGHT');
    } else {
        var targObj = getObjectByName(targetOrObj);
        if (!targObj) return;
        var worldTarget = targObj.getWorldPosition(_pGlob.vec3Tmp2);
    }

    duration = Math.max(0, duration);

    if (appInstance.controls && appInstance.controls.tween) {
        // orbit and flying cameras
        if (!appInstance.controls.inTween) {
            appInstance.controls.tween(worldPos, worldTarget, duration, doSlot,
                    movementType);
        }
    } else {
        // TODO: static camera, just position it for now
        if (camera.parent) {
            camera.parent.worldToLocal(worldPos);
        }
        camera.position.copy(worldPos);
        camera.lookAt(worldTarget);
        doSlot();
    }
}

/**
 * Get a scene that contains the root of the given action.
 */
function getSceneByAction(action) {
    var root = action.getRoot();
    var scene = root.type == "Scene" ? root : null;
    root.traverseAncestors(function(ancObj) {
        if (ancObj.type == "Scene") {
            scene = ancObj;
        }
    });
    return scene;
}

/**
 * Get the current scene's framerate.
 */
function getSceneAnimFrameRate(scene) {
    if (scene && 'animFrameRate' in scene.userData) {
        return scene.userData.animFrameRate;
    }
    return 24;
}

_pGlob.animMixerCallbacks = [];

var initAnimationMixer = function() {

    function onMixerFinished(e) {
        var cb = _pGlob.animMixerCallbacks;
        var found = [];
        for (var i = 0; i < cb.length; i++) {
            if (cb[i][0] == e.action) {
                cb[i][0] = null; // desactivate
                found.push(cb[i][1]);
            }
        }
        for (var i = 0; i < found.length; i++) {
            found[i]();
        }
    }

    return function initAnimationMixer() {
        if (appInstance.mixer && !appInstance.mixer.hasEventListener('finished', onMixerFinished)) {
            bindListener(appInstance.mixer, 'finished', onMixerFinished);
        }
    };

}();

// animation puzzles
function operateAnimation(operation, animations, from, to, loop, speed, callback, rev) {
    if (!animations)
        return;
    // input can be either single obj or array of objects
    if (typeof animations == "string")
        animations = [animations];

    function processAnimation(animName) {
        var action = v3d.SceneUtils.getAnimationActionByName(appInstance, animName);
        if (!action)
            return;
        switch (operation) {
        case 'PLAY':
            if (!action.isRunning()) {
                action.reset();
                if (loop && (loop != "AUTO"))
                    action.loop = v3d[loop];
                var scene = getSceneByAction(action);
                var frameRate = getSceneAnimFrameRate(scene);

                action.repetitions = Infinity;

                var timeScale = Math.abs(parseFloat(speed));
                if (rev)
                    timeScale *= -1;

                action.timeScale = timeScale;
                action.timeStart = from !== null ? from/frameRate : 0;
                if (to !== null) {
                    action.getClip().duration = to/frameRate;
                } else {
                    action.getClip().resetDuration();
                }
                action.time = timeScale >= 0 ? action.timeStart : action.getClip().duration;

                action.paused = false;
                action.play();

                // push unique callbacks only
                var callbacks = _pGlob.animMixerCallbacks;
                var found = false;

                for (var j = 0; j < callbacks.length; j++)
                    if (callbacks[j][0] == action && callbacks[j][1] == callback)
                        found = true;

                if (!found)
                    _pGlob.animMixerCallbacks.push([action, callback]);
            }
            break;
        case 'STOP':
            action.stop();

            // remove callbacks
            var callbacks = _pGlob.animMixerCallbacks;
            for (var j = 0; j < callbacks.length; j++)
                if (callbacks[j][0] == action) {
                    callbacks.splice(j, 1);
                    j--
                }

            break;
        case 'PAUSE':
            action.paused = true;
            break;
        case 'RESUME':
            action.paused = false;
            break;
        case 'SET_FRAME':
            var scene = getSceneByAction(action);
            var frameRate = getSceneAnimFrameRate(scene);
            action.time = from ? from/frameRate : 0;
            action.play();
            action.paused = true;
            break;
        case 'SET_SPEED':
            var timeScale = parseFloat(speed);
            action.timeScale = rev ? -timeScale : timeScale;
            break;
        }
    }

    for (var i = 0; i < animations.length; i++) {
        var animName = animations[i];
        if (animName)
            processAnimation(animName);
    }

    initAnimationMixer();
}

// utility function used by the whenClicked, whenHovered and whenDraggedOver puzzles
function initObjectPicking(callback, eventType, mouseDownUseTouchStart, mouseButtons) {

    var elem = appInstance.renderer.domElement;
    bindListener(elem, eventType, pickListener);

    if (eventType == 'mousedown') {

        var touchEventName = mouseDownUseTouchStart ? 'touchstart' : 'touchend';
        bindListener(elem, touchEventName, pickListener);

    } else if (eventType == 'dblclick') {

        var prevTapTime = 0;

        function doubleTapCallback(event) {

            var now = new Date().getTime();
            var timesince = now - prevTapTime;

            if (timesince < 600 && timesince > 0) {

                pickListener(event);
                prevTapTime = 0;
                return;

            }

            prevTapTime = new Date().getTime();
        }

        var touchEventName = mouseDownUseTouchStart ? 'touchstart' : 'touchend';
        bindListener(elem, touchEventName, doubleTapCallback);
    }

    var raycaster = new v3d.Raycaster();

    function pickListener(event) {

        // to handle unload in loadScene puzzle
        if (!appInstance.getCamera())
            return;

        event.preventDefault();

        var xNorm = 0, yNorm = 0;
        if (event instanceof MouseEvent) {
            if (mouseButtons && mouseButtons.indexOf(event.button) == -1)
                return;
            xNorm = event.offsetX / elem.clientWidth;
            yNorm = event.offsetY / elem.clientHeight;
        } else if (event instanceof TouchEvent) {
            var rect = elem.getBoundingClientRect();
            xNorm = (event.changedTouches[0].clientX - rect.left) / rect.width;
            yNorm = (event.changedTouches[0].clientY - rect.top) / rect.height;
        }

        _pGlob.screenCoords.x = xNorm * 2 - 1;
        _pGlob.screenCoords.y = -yNorm * 2 + 1;
        raycaster.setFromCamera(_pGlob.screenCoords, appInstance.getCamera(true));
        var objList = [];
        appInstance.scene.traverse(function(obj){objList.push(obj);});
        var intersects = raycaster.intersectObjects(objList, false);
        callback(intersects, event);
    }
}

function objectsIncludeObj(objNames, testedObjName) {
    if (!testedObjName) return false;

    for (var i = 0; i < objNames.length; i++) {
        if (testedObjName == objNames[i]) {
            return true;
        } else {
            // also check children which are auto-generated for multi-material objects
            var obj = getObjectByName(objNames[i]);
            if (obj && obj.type == "Group") {
                for (var j = 0; j < obj.children.length; j++) {
                    if (testedObjName == obj.children[j].name) {
                        return true;
                    }
                }
            }
        }
    }
    return false;
}

// utility function used by the whenClicked, whenHovered, whenDraggedOver, and raycast puzzles
function getPickedObjectName(obj) {
    // auto-generated from a multi-material object, use parent name instead
    if (obj.isMesh && obj.isMaterialGeneratedMesh && obj.parent) {
        return obj.parent.name;
    } else {
        return obj.name;
    }
}

// whenClicked puzzle
function registerOnClick(objSelector, xRay, doubleClick, mouseButtons, cbDo, cbIfMissedDo) {

    // for AR/VR
    _pGlob.objClickInfo = _pGlob.objClickInfo || [];

    _pGlob.objClickInfo.push({
        objSelector: objSelector,
        callbacks: [cbDo, cbIfMissedDo]
    });

    initObjectPicking(function(intersects, event) {

        var isPicked = false;

        var maxIntersects = xRay ? intersects.length : Math.min(1, intersects.length);

        for (var i = 0; i < maxIntersects; i++) {
            var obj = intersects[i].object;
            var objName = getPickedObjectName(obj);
            var objNames = retrieveObjectNames(objSelector);

            if (objectsIncludeObj(objNames, objName)) {
                // save the object for the pickedObject block
                _pGlob.pickedObject = objName;
                isPicked = true;
                cbDo(event);
            }
        }

        if (!isPicked) {
            _pGlob.pickedObject = '';
            cbIfMissedDo(event);
        }

    }, doubleClick ? 'dblclick' : 'mousedown', false, mouseButtons);
}

// show and hide puzzles
function changeVis(objSelector, bool) {
    var objNames = retrieveObjectNames(objSelector);

    for (var i = 0; i < objNames.length; i++) {
        var objName = objNames[i]
        if (!objName)
            continue;
        var obj = getObjectByName(objName);
        if (!obj)
            continue;
        obj.visible = bool;
        obj.resolveMultiMaterial().forEach(function(objR) {
            objR.visible = bool;
        });
    }
}

// stopSound puzzle
function stopSound(mediaElem) {
    const mediaElems = (Array.isArray(mediaElem) ? mediaElem : [mediaElem]).filter(elem => elem);
    mediaElems.forEach(mediaElem => {
        mediaElem.stop();
    });
}

// setHTMLElemAttribute puzzle
function setHTMLElemAttribute(attr, value, ids, isParent) {
    var elems = getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem) continue;

        if ((attr == 'href' || attr == 'src') && value instanceof Promise) {
            // resolve promise value for url-based attributes
            value.then(function(response) {
                elem[attr] = response;
            });
        } else {
            elem[attr] = value;
        }
    }
}


is_ft_loaded = true;
eventHTMLElem('click', 'btn_abrir_informacion', false, function(event) {
  setHTMLElemStyle('display', 'flex', 'info_screen', false);
});
eventHTMLElem('click', 'btn_cerrar_informacion', false, function(event) {
  if (is_ft_loaded == true) {
    is_ft_loaded = false;
    setHTMLElemStyle('display', 'flex', 'btn_abrir_informacion', false);
    setHTMLElemStyle('display', 'none', 'info_screen', false);
    playSound(loadMedia_HTML5('./audios/inicio.mp3'), false);
    playSound(loadMedia_HTML5('../audios/Feeder.mp3'), true);
    volume(loadMedia_HTML5('../audios/Feeder.mp3'), 0.03);
    setHTMLElemStyle('display', 'flex', 'nota_inicio', false);
    registerSetTimeout(5, function() {
      tweenCamera('Camara003', 'CamaraDireccion003', 1, function() {
        setHTMLElemStyle('display', 'none', 'nota_inicio', false);
        playSound(loadMedia_HTML5('./audios/nota_1.mp3'), false);
        registerSetTimeout(5, function() {
          setHTMLElemStyle('display', 'flex', 'btn_next_1', false);
        });
        setHTMLElemStyle('display', 'flex', 'nota_1', false);
        outline('Object7891', 'ENABLE');
      }, 0);
    });
  } else {
    setHTMLElemStyle('display', 'flex', 'btn_abrir_informacion', false);
    setHTMLElemStyle('display', 'none', 'info_screen', false);
  }
});

eventHTMLElem('click', 'btn_next_1', false, function(event) {
  registerSetTimeout(1, function() {});
  tweenCamera('Camara004', 'CamaraDireccion004', 1, function() {
    setHTMLElemStyle('display', 'none', 'btn_next_1', false);
    setHTMLElemStyle('display', 'none', 'nota_1', false);
    setHTMLElemStyle('display', 'flex', 'nota_2', false);
    playSound(loadMedia_HTML5('./audios/nota_2.mp3'), false);
    registerSetTimeout(8, function() {
      outline('BotonRojoApron001', 'ENABLE');
      is_press_red_button_apronfeeder = true;
      registerOnClick('BotonRojoApron001', false, false, [0,1,2], function() {
        if (is_press_red_button_apronfeeder == true) {
          is_press_red_button_apronfeeder = false;
          outline('BotonRojoApron001', 'DISABLE');

          operateAnimation('SET_FRAME', 'LuzVerdeApron001', 320, null, 'AUTO', 1,
                  function() {}, false);


          operateAnimation('SET_FRAME', 'LuzRojaApron001', 320, null, 'AUTO', 1,
                  function() {}, false);

              outline('BotonRojoApron', 'ENABLE');
          is_press_red_button_apronfeeder = true;
          registerOnClick('BotonRojoApron', false, false, [0,1,2], function() {
            if (is_press_red_button_apronfeeder == true) {
              is_press_red_button_apronfeeder = false;
              outline('BotonRojoApron', 'DISABLE');

              operateAnimation('SET_FRAME', 'LuzVerdeApron', 340, null, 'AUTO', 1,
                      function() {}, false);


              operateAnimation('SET_FRAME', 'LuzRojaApron', 340, null, 'AUTO', 1,
                      function() {}, false);

                  setHTMLElemStyle('display', 'none', 'nota_2', false);
              setHTMLElemStyle('display', 'flex', 'nota_3', false);
              playSound(loadMedia_HTML5('./audios/nota_3.mp3'), false);
              registerSetTimeout(4, function() {
                setHTMLElemStyle('display', 'flex', 'btn_next_3', false);
              });
              outline('Object7891', 'DISABLE');
            }
          }, function() {});
        }
      }, function() {});
    });
  }, 0);
});
eventHTMLElem('click', 'btn_next_3', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_3', false);
  setHTMLElemStyle('display', 'none', 'nota_3', false);
  setHTMLElemStyle('display', 'flex', 'nota_4', false);
  playSound(loadMedia_HTML5('./audios/nota_4.mp3'), false);
  tweenCamera('Camara006', 'CamaraDireccion006', 0.25, function() {
    setHTMLElemStyle('display', 'flex', 'checklist-elementos-bloqueo', false);
    eventHTMLElem('click', 'elemento_bloqueo_1', false, function(event) {
      changeVis('candado001', false);

      Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_1_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);

      setHTMLElemStyle('display', 'none', 'nota_4', false);
      stopSound(loadMedia_HTML5('./audios/nota_4.mp3'));
      setHTMLElemStyle('display', 'flex', 'nota_5', false);
      playSound(loadMedia_HTML5('./audios/nota_5.mp3'), false);
      setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_1', false);
      setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_2', false);
      eventHTMLElem('click', 'elemento_bloqueo_2', false, function(event) {
        changeVis('llave001', false);

        Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_2_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);

        setHTMLElemStyle('display', 'none', 'nota_5', false);
        stopSound(loadMedia_HTML5('./audios/nota_5.mp3'));
        setHTMLElemStyle('display', 'flex', 'nota_6', false);
        playSound(loadMedia_HTML5('./audios/nota_6.mp3'), false);
        setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_2', false);
        setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_3', false);
        eventHTMLElem('click', 'elemento_bloqueo_3', false, function(event) {
          changeVis('Pinza001', false);

          Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_3_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);

          setHTMLElemStyle('display', 'none', 'nota_6', false);
          stopSound(loadMedia_HTML5('./audios/nota_6.mp3'));
          setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_3', false);
          setHTMLElemStyle('display', 'flex', 'btn_next_7', false);
        });
      });
    });
  }, 0);
});
eventHTMLElem('click', 'btn_next_7', false, function(event) {
  stopSound(loadMedia_HTML5('./audios/nota_7.mp3'));
  setHTMLElemStyle('display', 'none', 'checklist-elementos-bloqueo', false);
  setHTMLElemStyle('display', 'none', 'btn_next_7', false);
  setHTMLElemStyle('display', 'none', 'nota_7', false);
  tweenCamera('Camara007', 'CamaraDireccion007', 1, function() {
    setHTMLElemStyle('display', 'flex', 'nota_8', false);
    playSound(loadMedia_HTML5('./audios/nota_8.mp3'), false);
    registerSetTimeout(4, function() {
      outline('mameluco', 'ENABLE');
      registerOnClick('mameluco', false, false, [0,1,2], function() {
        changeVis('mameluco', false);
        setHTMLElemStyle('display', 'none', 'nota_8', false);
        stopSound(loadMedia_HTML5('./audios/nota_8.mp3'));
        setHTMLElemStyle('display', 'flex', 'nota_9', false);
        playSound(loadMedia_HTML5('./audios/nota_9.mp3'), false);
        registerSetTimeout(3, function() {
          outline('casco', 'ENABLE');
          registerOnClick('casco', false, false, [0,1,2], function() {
            changeVis('casco', false);
            setHTMLElemStyle('display', 'none', 'nota_9', false);
            setHTMLElemStyle('display', 'flex', 'nota_10', false);
            playSound(loadMedia_HTML5('./audios/nota_10.mp3'), false);
            registerSetTimeout(4, function() {
              outline('GuantesGoma', 'ENABLE');
              registerOnClick('GuantesGoma', false, false, [0,1,2], function() {
                changeVis('GuantesGoma', false);
                setHTMLElemStyle('display', 'none', 'nota_10', false);
                setHTMLElemStyle('display', 'flex', 'nota_10_2', false);
                playSound(loadMedia_HTML5('./audios/nota_10_2.mp3'), false);
                registerSetTimeout(4, function() {
                  outline('GuantesCuero', 'ENABLE');
                  registerOnClick('GuantesCuero', false, false, [0,1,2], function() {
                    changeVis('GuantesCuero', false);
                    setHTMLElemStyle('display', 'none', 'nota_10_2', false);
                    setHTMLElemStyle('display', 'flex', 'nota_11', false);
                    playSound(loadMedia_HTML5('./audios/nota_11.mp3'), false);
                    registerSetTimeout(4, function() {
                      outline('BotasGoma', 'ENABLE');
                      registerOnClick('BotasGoma', false, false, [0,1,2], function() {
                        changeVis('BotasGoma', false);
                        setHTMLElemStyle('display', 'flex', 'btn_next_11', false);
                      }, function() {});
                    });
                  }, function() {});
                });
              }, function() {});
            });
          }, function() {});
        });
      }, function() {});
    });
  }, 0);
});
eventHTMLElem('click', 'btn_next_11', false, function(event) {
  playSound(loadMedia_HTML5('./audios/nota_12.mp3'), false);
  registerSetTimeout(5, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_12', false);
  });
  setHTMLElemStyle('display', 'none', 'btn_next_11', false);
  setHTMLElemStyle('display', 'none', 'nota_11', false);
  setHTMLElemStyle('display', 'flex', 'nota_12', false);
  tweenCamera('Camara008', 'CamaraDireccion008', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_12', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_12', false);
  setHTMLElemStyle('display', 'none', 'nota_12', false);
  setHTMLElemStyle('display', 'flex', 'nota_13', false);
  playSound(loadMedia_HTML5('./audios/nota_13.mp3'), false);
  registerSetTimeout(5, function() {
    is_bajar_palanca_apronfeeder = true;
    outline('palanca0002', 'ENABLE');
    registerOnClick('palanca0002', false, false, [0,1,2], function() {
      if (is_bajar_palanca_apronfeeder == true) {
        is_bajar_palanca_apronfeeder = false;
        outline('palanca0002', 'DISABLE');

        operateAnimation('PLAY', 'palanca0002', 830, 855, 'LoopOnce', 1,
                function() {

          operateAnimation('PLAY', 'LuzRojaApronGabinete', 854, 855, 'LoopOnce', 1,
                  function() {
            setHTMLElemStyle('display', 'flex', 'btn_next_13', false);
          }, false);

              }, false);

            }
    }, function() {});
  });
  tweenCamera('Camara008', 'CamaraDireccion008', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_13', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_13', false);
  setHTMLElemStyle('display', 'none', 'checklist-elementos-bloqueo-apronfeeder', false);
  setHTMLElemStyle('display', 'none', 'nota_13', false);
  tweenCamera('Camara008', 'CamaraDireccion008', 1, function() {
    setHTMLElemStyle('display', 'flex', 'checklist-elementos-bloqueo-apronfeeder', false);
    playSound(loadMedia_HTML5('./audios/nota_14.mp3'), false);
    setHTMLElemStyle('display', 'flex', 'nota_14', false);
    eventHTMLElem('click', 'elemento_bloqueo_apronfeeder_1', false, function(event) {

      Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_apronfeeder_1_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


      operateAnimation('PLAY', 'Pinza007', 859, 860, 'LoopOnce', 1,
              function() {
        setHTMLElemStyle('display', 'none', 'nota_14', false);
        stopSound(loadMedia_HTML5('./audios/nota_14.mp3'));
        setHTMLElemStyle('display', 'flex', 'nota_15', false);
        playSound(loadMedia_HTML5('./audios/nota_15.mp3'), false);
        setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_apronfeeder_1', false);
        setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_apronfeeder_2', false);
        eventHTMLElem('click', 'elemento_bloqueo_apronfeeder_2', false, function(event) {

          Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_apronfeeder_2_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


          operateAnimation('PLAY', 'candado007', 859, 860, 'LoopOnce', 1,
                  function() {
            setHTMLElemStyle('display', 'none', 'nota_15', false);
            stopSound(loadMedia_HTML5('./audios/nota_15.mp3'));
            setHTMLElemStyle('display', 'flex', 'nota_16', false);
            playSound(loadMedia_HTML5('./audios/nota_16.mp3'), false);
            setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_apronfeeder_2', false);
            setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_apronfeeder_3', false);
            eventHTMLElem('click', 'elemento_bloqueo_apronfeeder_3', false, function(event) {

              Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_apronfeeder_3_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


              operateAnimation('PLAY', 'Etiqueta007', 869, 870, 'LoopOnce', 1,
                      function() {
                setHTMLElemStyle('display', 'none', 'nota_16', false);
                setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_apronfeeder_3', false);
                setHTMLElemStyle('display', 'flex', 'btn_next_17', false);
              }, false);

                  });
          }, false);

              });
      }, false);

          });
  }, 0);
});
eventHTMLElem('click', 'btn_next_17', false, function(event) {
  stopSound(loadMedia_HTML5('./audios/nota_16.mp3'));
  setHTMLElemStyle('display', 'none', 'checklist-elementos-bloqueo-apronfeeder', false);

  operateAnimation('SET_FRAME', 'LuzRojaApron001', 0, null, 'AUTO', 1,
          function() {}, false);


  operateAnimation('SET_FRAME', 'LuzRojaApron', 0, null, 'AUTO', 1,
          function() {}, false);

      setHTMLElemStyle('display', 'none', 'btn_next_17', false);
  setHTMLElemStyle('display', 'none', 'nota_17', false);
  setHTMLElemStyle('display', 'flex', 'nota_18', false);
  playSound(loadMedia_HTML5('./audios/nota_18.mp3'), false);
  registerSetTimeout(5, function() {
    is_bajar_palanca_electroiman = true;
    outline('palanca0004', 'ENABLE');
    registerOnClick('palanca0004', false, false, [0,1,2], function() {
      if (is_bajar_palanca_electroiman == true) {
        is_bajar_palanca_electroiman = false;
        outline('palanca0004', 'DISABLE');

        operateAnimation('PLAY', 'palanca0004', 900, 925, 'LoopOnce', 1,
                function() {

          operateAnimation('SET_FRAME', 'LuzRojaElectroGabinete', 925, null, 'AUTO', 1,
                  function() {}, false);

              setHTMLElemStyle('display', 'flex', 'btn_next_18', false);
        }, false);

            }
    }, function() {});
  });
  tweenCamera('Camara008', 'CamaraDireccion008', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_18', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_18', false);
  setHTMLElemStyle('display', 'none', 'nota_18', false);
  tweenCamera('Camara008', 'CamaraDireccion008', 1, function() {
    setHTMLElemStyle('display', 'flex', 'checklist-elementos-bloqueo-electroiman', false);
    setHTMLElemStyle('display', 'flex', 'nota_19', false);
    playSound(loadMedia_HTML5('./audios/nota_19.mp3'), false);
    eventHTMLElem('click', 'elemento_bloqueo_electroiman_1', false, function(event) {

      Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_electroiman_1_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


      operateAnimation('PLAY', 'Pinza009', 929, 930, 'LoopOnce', 1,
              function() {
        setHTMLElemStyle('display', 'none', 'nota_19', false);
        stopSound(loadMedia_HTML5('./audios/nota_19.mp3'));
        setHTMLElemStyle('display', 'flex', 'nota_20', false);
        playSound(loadMedia_HTML5('./audios/nota_20.mp3'), false);
        setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_electroiman_1', false);
        setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_electroiman_2', false);
        eventHTMLElem('click', 'elemento_bloqueo_electroiman_2', false, function(event) {

          Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_electroiman_2_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


          operateAnimation('PLAY', 'candado009', 934, 935, 'LoopOnce', 1,
                  function() {
            setHTMLElemStyle('display', 'none', 'nota_20', false);
            stopSound(loadMedia_HTML5('./audios/nota_20.mp3'));
            setHTMLElemStyle('display', 'flex', 'nota_21', false);
            playSound(loadMedia_HTML5('./audios/nota_21.mp3'), false);
            setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_electroiman_2', false);
            setHTMLElemAttribute('style', 'pointer-events:initial;', 'elemento_bloqueo_electroiman_3', false);
            eventHTMLElem('click', 'elemento_bloqueo_electroiman_3', false, function(event) {

              Function('app', 'v3d', 'puzzles', 'VARS', 'PROC', 'document.getElementById("elemento_bloqueo_electroiman_3_input").checked = true;')(appInstance, v3d, PL, VARS, PROC);


              operateAnimation('PLAY', 'Etiqueta009', 939, 940, 'LoopOnce', 1,
                      function() {
                setHTMLElemStyle('display', 'none', 'nota_21', false);
                setHTMLElemAttribute('style', 'pointer-events: none;', 'elemento_bloqueo_electroiman_3', false);
                setHTMLElemStyle('display', 'flex', 'btn_next_21', false);
              }, false);

                  });
          }, false);

              });
      }, false);

          });
  }, 0);
});
eventHTMLElem('click', 'btn_next_21', false, function(event) {
  stopSound(loadMedia_HTML5('./audios/nota_21.mp3'));
  playSound(loadMedia_HTML5('./audios/nota_22.mp3'), false);
  registerSetTimeout(3, function() {
    is_press_tapa_lonchera = true;
    outline('TapaLonchera', 'ENABLE');
    registerOnClick('TapaLonchera', false, false, [0,1,2], function() {
      if (is_press_tapa_lonchera == true) {
        is_press_tapa_lonchera = false;
        outline('TapaLonchera', 'DISABLE');

        operateAnimation('PLAY', 'TapaLonchera', 1130, 1180, 'LoopOnce', 1,
                function() {
          setHTMLElemStyle('display', 'flex', 'btn_next_22', false);
        }, false);

            }
    }, function() {});
  });
  setHTMLElemStyle('display', 'none', 'checklist-elementos-bloqueo-electroiman', false);
  setHTMLElemStyle('display', 'none', 'btn_next_21', false);
  setHTMLElemStyle('display', 'none', 'nota_21', false);
  setHTMLElemStyle('display', 'flex', 'nota_22', false);
  tweenCamera('Camara009', 'CamaraDireccion009', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_22', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_22', false);
  setHTMLElemStyle('display', 'none', 'nota_22', false);
  setHTMLElemStyle('display', 'flex', 'nota_23', false);
  playSound(loadMedia_HTML5('./audios/nota_23.mp3'), false);
  registerSetTimeout(5, function() {
    setHTMLElemStyle('display', 'flex', 'btn_guardar_llaves', false);
    eventHTMLElem('click', 'btn_guardar_llaves', false, function(event) {
      setHTMLElemStyle('display', 'none', 'btn_guardar_llaves', false);

      operateAnimation('PLAY', 'Llave006', 1180, 1230, 'LoopOnce', 1,
              function() {

        operateAnimation('PLAY', 'TapaLonchera', 1230, 1280, 'LoopOnce', 1,
                function() {
          setHTMLElemStyle('display', 'flex', 'btn_next_23', false);
        }, false);

            }, false);

          });
  });
  tweenCamera('Camara009', 'CamaraDireccion009', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_23', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_23', false);
  setHTMLElemStyle('display', 'none', 'nota_23', false);
  setHTMLElemStyle('display', 'flex', 'nota_24', false);
  playSound(loadMedia_HTML5('./audios/nota_24.mp3'), false);
  registerSetTimeout(3, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_24', false);
  });
  tweenCamera('Camara009', 'CamaraDireccion009', 1, function() {

    operateAnimation('SET_FRAME', 'candado010', 1285, null, 'AUTO', 1,
            function() {}, false);


    operateAnimation('SET_FRAME', 'Etiqueta010', 1285, null, 'AUTO', 1,
            function() {}, false);

        }, 0);
});
eventHTMLElem('click', 'btn_next_24', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_24', false);
  setHTMLElemStyle('display', 'none', 'nota_24', false);
  setHTMLElemStyle('display', 'flex', 'nota_25', false);
  playSound(loadMedia_HTML5('./audios/nota_25.mp3'), false);
  registerSetTimeout(4, function() {
    outline('Llave005', 'ENABLE');
    registerOnClick('Llave005', false, false, [0,1,2], function() {
      changeVis('Llave005', false);
      setHTMLElemStyle('display', 'flex', 'btn_next_25', false);
    }, function() {});
  });
  tweenCamera('Camara009', 'CamaraDireccion009', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_25', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_25', false);
  setHTMLElemStyle('display', 'none', 'nota_25', false);
  setHTMLElemStyle('display', 'flex', 'nota_26', false);
  playSound(loadMedia_HTML5('./audios/nota_26.mp3'), false);
  registerSetTimeout(6, function() {
    outline('BotonVerdeApron001', 'ENABLE');
    is_press_red_button_apronfeeder = true;
    registerOnClick('BotonVerdeApron001', false, false, [0,1,2], function() {
      if (is_press_red_button_apronfeeder == true) {
        is_press_red_button_apronfeeder = false;
        outline('BotonVerdeApron001', 'DISABLE');
        outline('BotonVerdeApron', 'ENABLE');
        is_press_red_button_apronfeeder = true;
        registerOnClick('BotonVerdeApron', false, false, [0,1,2], function() {
          if (is_press_red_button_apronfeeder == true) {
            is_press_red_button_apronfeeder = false;
            outline('BotonVerdeApron', 'DISABLE');
            setHTMLElemStyle('display', 'flex', 'btn_next_26', false);
          }
        }, function() {});
      }
    }, function() {});
  });
  tweenCamera('Camara004', 'CamaraDireccion004', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_26', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_26', false);
  setHTMLElemStyle('display', 'none', 'nota_26', false);
  setHTMLElemStyle('display', 'flex', 'nota_27', false);
  playSound(loadMedia_HTML5('./audios/nota_27.mp3'), false);
  registerSetTimeout(7, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_27', false);
  });
  tweenCamera('Camara004', 'CamaraDireccion004', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_27', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_27', false);
  setHTMLElemStyle('display', 'none', 'nota_27', false);
  setHTMLElemStyle('display', 'flex', 'nota_28', false);
  playSound(loadMedia_HTML5('./audios/nota_28.mp3'), false);
  registerSetTimeout(5, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_28', false);
  });
  tweenCamera('Camara009', 'CamaraDireccion009', 0.25, function() {

    operateAnimation('SET_FRAME', 'candado011', 1390, null, 'AUTO', 1,
            function() {}, false);


    operateAnimation('SET_FRAME', 'Etiqueta011', 1390, null, 'AUTO', 1,
            function() {}, false);

        }, 0);
});
eventHTMLElem('click', 'btn_next_28', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_28', false);
  setHTMLElemStyle('display', 'none', 'nota_28', false);
  setHTMLElemStyle('display', 'flex', 'nota_29', false);
  playSound(loadMedia_HTML5('./audios/nota_29.mp3'), false);
  registerSetTimeout(7, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_borrar', false);
  });
  tweenCamera('Camara009', 'CamaraDireccion009', 1, function() {}, 0);
});
eventHTMLElem('click', 'btn_next_borrar', false, function(event) {
  playSound(loadMedia_HTML5('./audios/nota_final.mp3'), false);
  setHTMLElemStyle('display', 'none', 'btn_next_borrar', false);
  setHTMLElemStyle('display', 'none', 'nota_29', false);
  setHTMLElemStyle('display', 'flex', 'nota_img_final', false);
  registerSetTimeout(7, function() {
    setHTMLElemStyle('display', 'flex', 'btn_next_29', false);
  });
});
eventHTMLElem('click', 'btn_next_29', false, function(event) {
  setHTMLElemStyle('display', 'none', 'btn_next_29', false);
  setHTMLElemStyle('display', 'none', 'nota_29', false);
  setHTMLElemStyle('display', 'none', 'nota_img_final', false);
});



} // end of PL.init function

PL.disposeListeners = function() {
    if (_pGlob) {
        _pGlob.eventListeners.forEach(({ target, type, listener, optionsOrUseCapture }) => {
            target.removeEventListener(type, listener, optionsOrUseCapture);
        });
        _pGlob.eventListeners.length = 0;
    }
}

PL.dispose = function() {
    PL.disposeListeners();
    _pGlob = null;
    // backward compatibility
    if (v3d[Symbol.toStringTag] !== 'Module') {
        delete v3d.PL;
        delete v3d.puzzles;
    }
}



})(); // end of closure
